import { SetBackgroundDirective } from './set-background.directive';

describe('SetBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
